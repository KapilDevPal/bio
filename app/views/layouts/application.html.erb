<!DOCTYPE html>
<html>
  <head>
    <title>Marriage Biodata Generator</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Create beautiful marriage biodata with our free online generator. Customize, preview, and download your biodata in multiple languages.">
    <meta name="keywords" content="marriage biodata, biodata generator, matrimonial biodata, wedding biodata">
    <meta property="og:title" content="Marriage Biodata Generator">
    <meta property="og:description" content="Create beautiful marriage biodata with our free online generator.">
    <meta property="og:type" content="website">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
  </head>

  <body class="bg-gradient-to-br from-indigo-50 via-white to-purple-50 min-h-screen">
    <div class="min-h-screen">
      <!-- Header -->
      <header class="bg-white/80 backdrop-blur-md shadow-lg border-b border-indigo-200 sticky top-0 z-40">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center py-4">
            <div class="flex items-center">
              <div class="relative">
                <i class="fas fa-heart text-indigo-500 text-3xl mr-3 animate-pulse"></i>
                <div class="absolute -top-1 -right-1 w-3 h-3 bg-purple-400 rounded-full animate-bounce"></div>
              </div>
              <h1 class="text-2xl font-bold bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent">
                Marriage Biodata Generator
              </h1>
            </div>
            <div class="flex items-center space-x-4">
              <a href="<%= root_path %>" class="text-gray-600 hover:text-indigo-600 transition-all duration-300 hover:scale-105 flex items-center space-x-2 px-4 py-2 rounded-lg hover:bg-indigo-50">
                <i class="fas fa-home"></i>
                <span class="hidden sm:inline">Home</span>
              </a>
            </div>
          </div>
        </div>
      </header>

      <!-- Main Content -->
      <main class="flex-1">
        <% if notice %>
          <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
            <div class="bg-gradient-to-r from-emerald-50 to-teal-50 border border-emerald-200 text-emerald-800 px-6 py-4 rounded-xl shadow-lg backdrop-blur-sm">
              <div class="flex items-center">
                <i class="fas fa-check-circle mr-3 text-emerald-500 text-xl"></i>
                <span class="font-medium"><%= notice %></span>
              </div>
            </div>
          </div>
        <% end %>

        <% if alert %>
          <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
            <div class="bg-gradient-to-r from-red-50 to-pink-50 border border-red-200 text-red-800 px-6 py-4 rounded-xl shadow-lg backdrop-blur-sm">
              <div class="flex items-center">
                <i class="fas fa-exclamation-circle mr-3 text-red-500 text-xl"></i>
                <span class="font-medium"><%= alert %></span>
              </div>
            </div>
          </div>
        <% end %>

        <%= yield %>
      </main>

      <!-- Footer -->
      <footer class="bg-white/80 backdrop-blur-md border-t border-indigo-200 mt-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
          <div class="text-center">
            <div class="flex justify-center items-center mb-4">
              <i class="fas fa-heart text-indigo-500 text-2xl mr-2 animate-pulse"></i>
              <span class="text-gray-600 font-medium">Made with love for your special moments</span>
            </div>
            <p class="text-gray-500 text-sm">
              &copy; <%= Date.current.year %> Marriage Biodata Generator. All rights reserved.
            </p>
          </div>
        </div>
      </footer>
    </div>
  </body>
</html>
